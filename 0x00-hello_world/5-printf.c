#include <stdio.h>

/**
 * main - This is the main function to return no argument
 *
 * Return: always 0
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,");
	return (0);
}
